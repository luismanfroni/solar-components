{
  "name": "root",
  "private": true,
  "scripts": {
    "publish-latest": "lerna publish -y --force-publish=* --conventional-commits",
    "publish-alpha": "lerna publish -y --force-publish=* --canary",
    "postinstall": "lerna bootstrap"
  },
  "devDependencies": {
    "@commitlint/cli": "^8.3.5",
    "@commitlint/config-conventional": "^8.3.4",
    "conventional-changelog": "^3.1.3",
    "husky": "^2.1.0",
    "lerna": "^3.20.2"
  }
}
